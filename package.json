{
  "name": "opticontent",
  "version": "1.0.0",
  "description": "MERN Stack Project with Gemini AI Integration",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "proxy": "node proxy.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
